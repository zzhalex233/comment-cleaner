<Window x:Class="CommentCleanerWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Comment Cleaner (WPF)"
        Width="1200"
        Height="780"
        MinWidth="1100"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

  <Window.Resources>

    <!-- ComboBox hover/selected（接近 Windows） -->
    <Color x:Key="HoverLight">#FFEAECEF</Color>
    <Color x:Key="SelectedLight">#FFDDE7F7</Color>

    <Color x:Key="HoverDark">#FF2A2D2E</Color>
    <Color x:Key="SelectedDark">#FF2F3B52</Color>

    <SolidColorBrush x:Key="HoverBrush"
                     Color="{StaticResource HoverLight}"/>
    <SolidColorBrush x:Key="SelectedBrush"
                     Color="{StaticResource SelectedLight}"/>

    <!-- Light palette -->
    <Color x:Key="BgLight">#FFFFFFFF</Color>
    <Color x:Key="PanelLight">#FFF3F4F6</Color>
    <Color x:Key="FgLight">#FF1F2328</Color>
    <Color x:Key="MutedLight">#FF57606A</Color>
    <Color x:Key="BorderLight">#FFD0D7DE</Color>
    <Color x:Key="FieldLight">#FFFFFFFF</Color>

    <!-- Dark palette (Windows-ish) -->
    <Color x:Key="BgDark">#FF1E1E1E</Color>
    <Color x:Key="PanelDark">#FF252526</Color>
    <Color x:Key="FgDark">#FFE6E6E6</Color>
    <Color x:Key="MutedDark">#FFB3B3B3</Color>
    <Color x:Key="BorderDark">#FF3A3A3A</Color>
    <Color x:Key="FieldDark">#FF2B2B2B</Color>

    <!-- diff delete background -->
    <Color x:Key="DelBgLight">#FFFFEEF0</Color>
    <Color x:Key="DelBgDark">#FF3B2A2A</Color>

    <!-- Accent（描边淡蓝，接近 Windows） -->
    <Color x:Key="AccentLight">#FF3A86FF</Color>
    <Color x:Key="AccentDark">#FF4FA3FF</Color>

    <!-- dynamic brushes (we swap them in code) -->
    <SolidColorBrush x:Key="BgBrush"
                     Color="{StaticResource BgLight}"/>
    <SolidColorBrush x:Key="PanelBrush"
                     Color="{StaticResource PanelLight}"/>
    <SolidColorBrush x:Key="FgBrush"
                     Color="{StaticResource FgLight}"/>
    <SolidColorBrush x:Key="MutedBrush"
                     Color="{StaticResource MutedLight}"/>
    <SolidColorBrush x:Key="BorderBrush"
                     Color="{StaticResource BorderLight}"/>
    <SolidColorBrush x:Key="FieldBrush"
                     Color="{StaticResource FieldLight}"/>
    <SolidColorBrush x:Key="DelBrush"
                     Color="{StaticResource DelBgLight}"/>
    <SolidColorBrush x:Key="AccentBrush"
                     Color="{StaticResource AccentLight}"/>

    <Style TargetType="{x:Type TextBlock}">
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="FontFamily"
              Value="Segoe UI"/>
      <Setter Property="FontSize"
              Value="13"/>
    </Style>

    <!-- TextBox -->
    <Style TargetType="{x:Type TextBox}">
      <Setter Property="FontFamily"
              Value="Consolas"/>
      <Setter Property="FontSize"
              Value="13"/>
      <Setter Property="Background"
              Value="{DynamicResource FieldBrush}"/>
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="BorderBrush"
              Value="{DynamicResource BorderBrush}"/>
      <Setter Property="VerticalContentAlignment"
              Value="Center"/>
      <Setter Property="Padding"
              Value="8,6"/>
      <Setter Property="SnapsToDevicePixels"
              Value="True"/>
      <Setter Property="CaretBrush"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="SelectionBrush"
              Value="{DynamicResource SelectedBrush}"/>
    </Style>

    <!-- ComboBox -->
    <Style x:Key="ThemedComboBoxItem"
           TargetType="{x:Type ComboBoxItem}">
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="Background"
              Value="Transparent"/>
      <Setter Property="Padding"
              Value="10,6"/>
      <Setter Property="HorizontalContentAlignment"
              Value="Stretch"/>
      <Setter Property="VerticalContentAlignment"
              Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ComboBoxItem}">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    CornerRadius="6"
                    SnapsToDevicePixels="True">
              <ContentPresenter Margin="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                RecognizesAccessKey="True"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="Background"
                        Value="{DynamicResource HoverBrush}"/>
              </Trigger>
              <Trigger Property="IsSelected"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="Background"
                        Value="{DynamicResource SelectedBrush}"/>
              </Trigger>
              <Trigger Property="IsEnabled"
                       Value="False">
                <Setter Property="Opacity"
                        Value="0.55"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ThemedComboBox"
           TargetType="{x:Type ComboBox}">
      <Setter Property="SnapsToDevicePixels"
              Value="True"/>
      <Setter Property="OverridesDefaultStyle"
              Value="True"/>
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
              Value="Disabled"/>
      <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
              Value="Auto"/>
      <Setter Property="MinHeight"
              Value="32"/>

      <Setter Property="Background"
              Value="{DynamicResource FieldBrush}"/>
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="BorderBrush"
              Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness"
              Value="1"/>
      <Setter Property="Padding"
              Value="10,4"/>
      <Setter Property="VerticalContentAlignment"
              Value="Center"/>
      <Setter Property="ItemContainerStyle"
              Value="{StaticResource ThemedComboBoxItem}"/>

      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ComboBox}">
            <Grid>
              <Border x:Name="Outer"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="8"
                      SnapsToDevicePixels="True">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="36"/>
                  </Grid.ColumnDefinitions>

                  <ContentPresenter x:Name="ContentSite"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    HorizontalAlignment="Left"
                                    Content="{TemplateBinding SelectionBoxItem}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    RecognizesAccessKey="True"/>

                  <Border Grid.Column="1"
                          Background="Transparent"
                          CornerRadius="0,8,8,0">
                    <Path Data="M 0 0 L 4 4 L 8 0 Z"
                          Fill="{DynamicResource MutedBrush}"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                  </Border>
                </Grid>
              </Border>

              <!-- ToggleButton：空模板避免 hover 填充 -->
              <ToggleButton x:Name="DropDownToggle"
                            Focusable="False"
                            ClickMode="Press"
                            Background="Transparent"
                            BorderThickness="0"
                            IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                <ToggleButton.Template>
                  <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="Transparent"/>
                  </ControlTemplate>
                </ToggleButton.Template>
              </ToggleButton>

              <Popup x:Name="PART_Popup"
                     Placement="Bottom"
                     IsOpen="{TemplateBinding IsDropDownOpen}"
                     AllowsTransparency="True"
                     Focusable="False"
                     PopupAnimation="Fade">
                <Border Background="{DynamicResource FieldBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10"
                        SnapsToDevicePixels="True"
                        Margin="0,6,0,0">
                  <ScrollViewer Margin="2"
                                SnapsToDevicePixels="True"
                                CanContentScroll="True"
                                VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Disabled">
                    <ItemsPresenter/>
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>

            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled"
                       Value="False">
                <Setter TargetName="Outer"
                        Property="Opacity"
                        Value="0.55"/>
              </Trigger>

              <Trigger Property="IsMouseOver"
                       Value="True">
                <Setter TargetName="Outer"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsKeyboardFocusWithin"
                       Value="True">
                <Setter TargetName="Outer"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsDropDownOpen"
                       Value="True">
                <Setter TargetName="Outer"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBox}"
           BasedOn="{StaticResource ThemedComboBox}"/>

    <!-- CheckBox -->
    <Style TargetType="{x:Type CheckBox}">
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="FontFamily"
              Value="Segoe UI"/>
      <Setter Property="FontSize"
              Value="13"/>
      <Setter Property="SnapsToDevicePixels"
              Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type CheckBox}">
            <Grid Margin="0,2">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>

              <Border x:Name="Box"
                      Width="16"
                      Height="16"
                      CornerRadius="3"
                      BorderThickness="1"
                      BorderBrush="{DynamicResource BorderBrush}"
                      Background="{DynamicResource FieldBrush}"
                      VerticalAlignment="Center"/>

              <Path x:Name="Mark"
                    Grid.Column="0"
                    Data="M 3 8 L 7 12 L 13 4"
                    StrokeThickness="2"
                    Stroke="{DynamicResource BgBrush}"
                    StrokeEndLineCap="Round"
                    StrokeStartLineCap="Round"
                    Visibility="Collapsed"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"/>

              <ContentPresenter Grid.Column="1"
                                Margin="8,0,0,0"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"/>
            </Grid>

            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver"
                       Value="True">
                <Setter TargetName="Box"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>

              <Trigger Property="IsChecked"
                       Value="True">
                <Setter TargetName="Box"
                        Property="Background"
                        Value="{DynamicResource AccentBrush}"/>
                <Setter TargetName="Box"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
                <Setter TargetName="Mark"
                        Property="Visibility"
                        Value="Visible"/>
              </Trigger>

              <Trigger Property="IsEnabled"
                       Value="False">
                <Setter Property="Opacity"
                        Value="0.55"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- GroupBox -->
    <Style TargetType="{x:Type GroupBox}">
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="BorderBrush"
              Value="{DynamicResource BorderBrush}"/>
      <Setter Property="Background"
              Value="{DynamicResource PanelBrush}"/>
      <Setter Property="Margin"
              Value="0,0,0,12"/>
      <Setter Property="Padding"
              Value="10"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type GroupBox}">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>

              <Border Background="Transparent"
                      Padding="6,0,6,6">
                <ContentPresenter ContentSource="Header"
                                  RecognizesAccessKey="True"
                                  Margin="4,0,0,0"/>
              </Border>

              <Border Grid.Row="1"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="1"
                      CornerRadius="10"
                      SnapsToDevicePixels="True">
                <ContentPresenter Margin="{TemplateBinding Padding}"/>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Button -->
    <Style TargetType="{x:Type Button}">
      <Setter Property="FontFamily"
              Value="Segoe UI"/>
      <Setter Property="FontSize"
              Value="13"/>
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="Background"
              Value="{DynamicResource PanelBrush}"/>
      <Setter Property="BorderBrush"
              Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness"
              Value="1"/>
      <Setter Property="Padding"
              Value="12,6"/>
      <Setter Property="Margin"
              Value="0,0,8,0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>
              <Trigger Property="IsPressed"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
                <Setter TargetName="Bd"
                        Property="Opacity"
                        Value="0.92"/>
              </Trigger>
              <Trigger Property="IsEnabled"
                       Value="False">
                <Setter TargetName="Bd"
                        Property="Opacity"
                        Value="0.55"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type TabItem}">
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="FontFamily"
              Value="Segoe UI"/>
      <Setter Property="FontSize"
              Value="13"/>
      <Setter Property="FocusVisualStyle"
              Value="{x:Null}"/>
      <Setter Property="SnapsToDevicePixels"
              Value="True"/>
      <Setter Property="UseLayoutRounding"
              Value="True"/>

      <Setter Property="MinWidth"
              Value="0"/>
      <Setter Property="HorizontalAlignment"
              Value="Left"/>

      <Setter Property="BorderThickness"
              Value="2"/>
      <Setter Property="Padding"
              Value="11,5"/>

      <Setter Property="Margin"
              Value="0,0,10,0"/>

      <Setter Property="Background"
              Value="Transparent"/>
      <Setter Property="BorderBrush"
              Value="Transparent"/>

      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type TabItem}">
            <Border Background="Transparent"
                    Padding="2"
                    SnapsToDevicePixels="True"
                    UseLayoutRounding="True"
                    ClipToBounds="False">
              <Border x:Name="Bd"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="8"
                      Padding="{TemplateBinding Padding}"
                      SnapsToDevicePixels="True"
                      UseLayoutRounding="True"
                      ClipToBounds="False">
                <ContentPresenter ContentSource="Header"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  RecognizesAccessKey="True"/>
              </Border>
            </Border>

            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>

              <Trigger Property="IsSelected"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="Background"
                        Value="{DynamicResource PanelBrush}"/>
                <Setter TargetName="Bd"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>

              <Trigger Property="IsKeyboardFocusWithin"
                       Value="True">
                <Setter TargetName="Bd"
                        Property="BorderBrush"
                        Value="{DynamicResource AccentBrush}"/>
              </Trigger>

              <Trigger Property="IsEnabled"
                       Value="False">
                <Setter Property="Opacity"
                        Value="0.55"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type TabControl}">
      <Setter Property="Background"
          Value="{DynamicResource BgBrush}"/>
      <Setter Property="BorderBrush"
          Value="{DynamicResource BorderBrush}"/>
      <Setter Property="BorderThickness"
          Value="1"/>
      <Setter Property="Padding"
          Value="10"/>
      <Setter Property="SnapsToDevicePixels"
          Value="True"/>
      <Setter Property="UseLayoutRounding"
          Value="True"/>

      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type TabControl}">
            <Grid SnapsToDevicePixels="True"
                  UseLayoutRounding="True"
                  ClipToBounds="False">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>

              <Border Grid.Row="0"
                      Background="Transparent"
                      Padding="0,0,30,10"
                      ClipToBounds="False"
                      Panel.ZIndex="10">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Disabled"
                              CanContentScroll="False"
                              ClipToBounds="False">
                  <TabPanel IsItemsHost="True"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            ClipToBounds="False"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True"/>
                </ScrollViewer>
              </Border>

              <Border Grid.Row="1"
                      Margin="0,1,0,0"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="10"
                      SnapsToDevicePixels="True"
                      ClipToBounds="False"
                      Panel.ZIndex="0">
                <ContentPresenter Margin="{TemplateBinding Padding}"
                                  ContentSource="SelectedContent"/>
              </Border>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="{x:Type DataGrid}">
      <Setter Property="Background"
              Value="{DynamicResource BgBrush}"/>
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="BorderBrush"
              Value="{DynamicResource BorderBrush}"/>
      <Setter Property="RowBackground"
              Value="{DynamicResource BgBrush}"/>
      <Setter Property="AlternatingRowBackground"
              Value="{DynamicResource PanelBrush}"/>
      <Setter Property="GridLinesVisibility"
              Value="None"/>
      <Setter Property="CanUserSortColumns"
              Value="False"/>
      <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
              Value="Auto"/>
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
              Value="Auto"/>
    </Style>

    <!-- 去掉 DataGrid 焦点框 -->
    <Style TargetType="{x:Type DataGridCell}">
      <Setter Property="FocusVisualStyle"
              Value="{x:Null}"/>
      <Setter Property="BorderThickness"
              Value="0"/>
      <Setter Property="Padding"
              Value="8,4"/>
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="Background"
              Value="Transparent"/>
    </Style>

    <Style TargetType="{x:Type DataGridRow}">
      <Setter Property="Background"
              Value="Transparent"/>
      <Setter Property="BorderThickness"
              Value="0"/>
      <Setter Property="FocusVisualStyle"
              Value="{x:Null}"/>
    </Style>

    <Style TargetType="{x:Type DataGridColumnHeader}">
      <Setter Property="Background"
              Value="{DynamicResource PanelBrush}"/>
      <Setter Property="Foreground"
              Value="{DynamicResource FgBrush}"/>
      <Setter Property="BorderBrush"
              Value="{DynamicResource BorderBrush}"/>
      <Setter Property="Padding"
              Value="8,6"/>
    </Style>

  </Window.Resources>

  <DockPanel Background="{DynamicResource BgBrush}"
             LastChildFill="True"
             Margin="14">

    <!-- Top bar -->
    <StackPanel DockPanel.Dock="Top"
                Orientation="Horizontal"
                Margin="0,0,0,12">
      <TextBlock Text="主题："
                 VerticalAlignment="Center"/>
      <ComboBox x:Name="ThemeCombo"
                Width="120"
                Margin="8,0,20,0"
                SelectedIndex="0">
        <ComboBoxItem Content="浅色"/>
        <ComboBoxItem Content="深色"/>
      </ComboBox>

      <TextBlock Text="模式："
                 VerticalAlignment="Center"/>
      <ComboBox x:Name="ModeCombo"
                Width="140"
                Margin="8,0,20,0"
                SelectedIndex="0">
        <ComboBoxItem Content="文件夹模式"/>
        <ComboBoxItem Content="选择文件模式"/>
      </ComboBox>

      <TextBlock Text="后缀："
                 VerticalAlignment="Center"/>
      <TextBox x:Name="SuffixBox"
               Width="420"
               Margin="8,0,12,0"
               Text=".cpp,.h,.java,.py"/>
      <Button Content="填入常用"
              Click="FillCommon_Click"/>
    </StackPanel>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Input -->
      <GroupBox Header="1) 输入">
        <Grid Margin="10">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <TextBlock Text="路径："
                     VerticalAlignment="Center"/>
          <TextBox x:Name="PathBox"
                   Grid.Column="1"
                   Margin="8,0,12,0"
                   IsReadOnly="True"/>
          <Button x:Name="PickFolderBtn"
                  Grid.Column="2"
                  Content="选择文件夹..."
                  Click="PickFolder_Click"/>
          <Button x:Name="PickFilesBtn"
                  Grid.Column="3"
                  Content="选择文件..."
                  Click="PickFiles_Click"/>
        </Grid>
      </GroupBox>

      <!-- Options -->
      <GroupBox Grid.Row="1"
                Header="2) 选项">
        <Grid Margin="10">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <StackPanel>
            <CheckBox x:Name="DryRunCheck"
                      Content="预览模式（Dry-run：不写回）"
                      IsChecked="True"
                      Margin="0,0,0,8"/>
            <CheckBox x:Name="BackupCheck"
                      Content="写回时自动备份到 _comment_cleaner_backup/"
                      IsChecked="True"
                      Margin="0,0,0,8"/>
            <CheckBox x:Name="IgnoreHiddenCheck"
                      Content="忽略隐藏文件/目录（文件夹模式）"
                      IsChecked="True"/>
          </StackPanel>

          <StackPanel Grid.Column="1">
            <CheckBox x:Name="ClearCommentOnlyLinesCheck"
                      Content="清空纯注释行（保留换行，不移动后续代码）"
                      IsChecked="True"
                      Margin="0,0,0,8"/>
            <CheckBox x:Name="DeleteClearedLinesCheck"
                      Content="同时删除这些行（会改变行号，让后续代码上移）"
                      IsChecked="False"/>
          </StackPanel>
        </Grid>
      </GroupBox>

      <!-- Bottom -->
      <Grid Grid.Row="2">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    Margin="0,0,0,10">
          <Button Content="开始运行"
                  Click="Run_Click"/>
          <Button Content="清空日志"
                  Click="ClearLog_Click"/>

          <TextBlock Text="变更文件："
                     VerticalAlignment="Center"
                     Margin="14,0,6,0"
                     Foreground="{DynamicResource MutedBrush}"/>
          <ComboBox x:Name="ChangedFileCombo"
                    Width="520"
                    SelectionChanged="ChangedFileCombo_SelectionChanged"/>

          <TextBlock x:Name="StatusText"
                     Text="就绪"
                     VerticalAlignment="Center"
                     Margin="14,0,0,0"
                     Foreground="{DynamicResource MutedBrush}"/>
        </StackPanel>

        <TabControl Grid.Row="1">
          <TabItem Header="日志">
            <TextBox x:Name="LogBox"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     IsReadOnly="True"/>
          </TabItem>

          <TabItem Header="Unified Diff">
            <TextBox x:Name="UnifiedDiffBox"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     IsReadOnly="True"/>
          </TabItem>

          <TabItem Header="Side-by-side（行对比）">
            <DataGrid x:Name="SideGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      CanUserResizeColumns="True"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="False"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Before（原）"
                                    Binding="{Binding Left}"
                                    Width="*"
                                    CanUserSort="False"/>
                <DataGridTextColumn Header="After（清理后）"
                                    Binding="{Binding Right}"
                                    Width="*"
                                    CanUserSort="False"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
        </TabControl>
      </Grid>

    </Grid>
  </DockPanel>
</Window>